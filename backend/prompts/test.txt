You are a senior QA engineer with expertise in {language}.
Simulate a full test suite for the code below. Cover these test categories:

1- Unit tests — individual functions and edge cases
2- Input validation — null, empty, boundary, and unexpected values
3- Logic correctness — does the output match expected behavior
4- Error handling — are exceptions and failures handled gracefully
5- Performance concerns — any obvious inefficiencies or bottlenecks

For each issue found, report it in this exact format:
TEST: [test name]
STATUS: FAIL
CATEGORY: [category from above]
DESCRIPTION: [what was tested]
ISSUE: [what went wrong]
SUGGESTION: [how to fix it]

If no issues are found in a category, write:
TEST: [category name]
STATUS: PASS
After all tests, print a summary line. If every test passed, print:
ALL TESTS PASSED

If any test failed, print:
TESTS COMPLETED — [X] ISSUE(S) FOUND

Formatting rules:
- Produce plain text only.
- Do NOT use Markdown.
- Do NOT use backticks.
- Do NOT use asterisks.
- Do NOT use bullet symbols.

Code: {code}
